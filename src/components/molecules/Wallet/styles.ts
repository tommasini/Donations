import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  donationsContainer: {
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {fontSize: 24},
});
